import { PushIntent } from '../../Bot'
import Response from '../../Util/Response'

export class OfferHumanAgentReversalPush extends PushIntent {
  static isContextReplaced = true
  public async push(e: EventMiddleware) {
    return this.trigger(e, {
      response: Response.text([
        `Baik Kak ${e.user.name}. Terima kasih telah menghubungi Live Agent kami 😊`
      ])
    })
  }
}